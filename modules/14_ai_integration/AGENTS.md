# Module 14 Codex Instructions

Scope: `modules/14_ai_integration/` only.

- Read root `AGENTS.md`, `docs/protocol.md`, `docs/module_contracts.md`, and `docs/tasks/module_14_ai_integration.md` before implementation.
- You may edit only this module directory unless Manager approves an interface RFC.
- Public API changes require `docs/api_contract.md`, module issue updates, and an RFC in root coordination docs.
- Local checks from repository root: `cmake -S . -B build -DCMAKE_BUILD_TYPE=Debug`, `cmake --build build`, `ctest --test-dir build --output-on-failure`, `python3 tools/check_governance.py`.
- Update module issues and README indexes when work starts or finishes.
- Record stubs in `docs/limitations.md`; TODO comments must include an issue id.
